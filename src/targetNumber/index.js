/**
 * 더하거나 빼기 두가지의 경우의 수만 존재
 * 음수로 변환한 수를 1개씩 추가하면서 모두 더하기 반복
 * 타겟 숫자가 나오면 count
 */
function solution(numbers, target) {
  var answer = 0;

  let check = [];
  let visit = [];

  visit.push(numbers[0]);

  return answer;
}

const numbers = [1, 1, 1, 1, 1];
const target = 3;
solution(numbers, target);
